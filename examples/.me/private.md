# Private

Notes that are **never exposed to AI tools by default**. To change that,
set `"private.md": true` under `permissions` in ~/.me/manifest.json.

- Negotiating a contract-to-hire offer from Meridian Labs — haven't decided, don't reference this anywhere.
- Plantfolio runway: ~7 months at current burn.
